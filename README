一、项目简介
本项目基于 MIT xv6 操作系统，通过注释和调试输出深入理解操作系统从 BIOS 加载到内核初始化的完整启动流程。

二、启动流程说明
BIOS 阶段
计算机加电，CPU 从 0xFFFF0 开始执行 BIOS 代码
BIOS 进行硬件自检，加载引导扇区到 0x7C00

Bootloader 阶段 (boot/bootasm.S)
实模式初始化：关闭中断，清零段寄存器
启用 A20：突破 1MB 内存限制
切换保护模式：加载 GDT，设置 CR0.PE 位
调用 bootmain：跳转到 C 代码

内核加载阶段 (boot/bootmain.c)
读取 ELF 头：从磁盘读取内核镜像头部
验证 ELF 格式：检查魔数 0x7F 45 4C 46
加载程序段：按程序头表将各段加载到指定内存
跳转内核：转入内核入口点 entry

内核初始化阶段 (entry.S)
建立页表：设置初始内核页表
物理地址 0~4MB 映射到虚拟地址 0x80000000~0x80400000
启用分页：设置 CR3 和 CR0.PG 位
设置栈指针：准备 C 函数调用环境
调用 main()：进入内核主函数

三、运行结果
编译成功：执行 make 命令成功生成 xv6.img（5.1MB）、kernel（71KB）、bootblock（512字节）等文件。
系统正常启动：执行 make qemu-nox 后，系统成功引导并进入 shell 提示符 $。
启动过程可视化：在启动过程中看到添加的调试信息：[KERNEL] main() started 和 [KERNEL] xv6 kernel is booting...。
任务验证：三个启动文件（bootasm.S、bootmain.c、entry.S）已添加详细注释，bootmain.c 中包含 [BOOT] 阶段标记。
Git提交记录：完成4次有意义提交，满足至少3次的要求。
所有实践任务要求均已成功完成，xv6 操作系统从引导加载到内核初始化的完整启动流程可正常运行和验证。

四、关键学习点
实模式 vs 保护模式
实模式：16位，直接访问物理内存，无内存保护
保护模式：32位，通过段描述符访问内存，支持特权级

分段 vs 分页
分段：bootasm.S 中通过 GDT 实现
分页：entry.S 中建立两级页表（页目录+页表）

ELF 文件格式
程序头表指定各段的加载位置
文件偏移、虚拟地址、物理地址的转换

虚拟地址空间布局
内核代码：0x80100000 开始
内核栈：0x80000000 附近
物理内存：通过 0x80000000 偏移访问

五、个人总结
通过本次实践，我深入理解了：
计算机启动的完整链条：BIOS → Bootloader → Kernel
汇编与 C 的协同工作方式
内存管理单元（MMU）的初始化过程
如何通过调试输出追踪启动流程
最让我印象深刻的是从实模式到保护模式的切换过程，以及虚拟地址空间的建立。这让我明白了现代操作系统如何通过硬件支持实现内存保护和多任务隔离。

六、Commit 记录
feat: initial xv6 setup and successful boot 基础环境搭建
docs: add detailed comments to boot process files 添加详细注释
feat: add boot process visualization with debug prints 添加调试输出
